{
  "name": "aetheris-addons",
  "version": "1.0.0",
  "private": true,
  "description": "Aetheris modules and integrations: store, contracts, templates and accepted pull requests.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "validate": "python tools/validate.py",
    "test": "python -m pytest -q"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
